{
  "name": "pdf",
  "version": "1.0.0",
  "title": "PDF Reader",
  "title_zh": "PDF 读取",
  "description": "Extract text from PDF files inside the local working directory.",
  "description_zh": "读取本地可操作目录内的 PDF 文件并提取文本。",
  "entry": "skill.py",
  "dependencies": ["pypdf>=5.0,<6.0"],
  "tools": {
    "pdf_read": {
      "description": "读取 PDF 文件并提取文本内容，用于总结、问答或搜索文档。",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "本地可操作目录内的 PDF 文件相对路径"
          },
          "from_page": {
            "type": "integer",
            "description": "起始页（从 1 开始），可选",
            "default": 1
          },
          "to_page": {
            "type": "integer",
            "description": "结束页（含），可选"
          }
        },
        "required": ["path"]
      }
    }
  }
}
